[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "roche"
version = "0.2.8"
requires-python = ">=3.8"
readme = "README.md"

[tool.maturin]
features = ["pyo3/extension-module", "pyo3/abi3-py38"]